---
title: "API reference"
description: "Every endpoint, its real status, and where the full schema lives. Generated from the capability matrix."
---

{/* Generated by tools/sync-status.mjs from api-spec/status.json + api-spec/libertynet-v1.yaml. Do not edit by hand — run `node tools/sync-status.mjs` instead. CI fails if this drifts. */}

import { Status } from '/snippets/status.mdx';

The full OpenAPI 3.1 document is at
[`/api-spec/libertynet-v1.yaml`](/api-spec/libertynet-v1.yaml) — schemas, examples and
all. This page is the index, with each operation carrying the same status as every
other surface in this project.

<Warning>
A status here is not decoration. **Implemented** can be called right now.
**Not yet wired** answers with placeholder zeros that must never be shown as
measurements. **Testing** is not deployed anywhere public. **Planned** has nothing
behind it — code written against it cannot work.
</Warning>

## Node discovery

**Base URL** `https://registry.libertynet.ai` · **Auth** none

### `GET /health` <Status level="implemented" />

Registry liveness

Registry liveness + registered node count.

```bash
curl -s https://registry.libertynet.ai/health
```

### `GET /nodes` <Status level="implemented" />

List all registered nodes

Full node table as JSON. Each record carries computed `reachability` (public|private|unroutable) and `signature_present`. Use `?callable=1` for only publicly-reachable, signed nodes — today that is 1 of 28.

```bash
curl -s https://registry.libertynet.ai/nodes
```

### `GET /peers` <Status level="implemented" />

List peers as plain text

Plain-text peer list (did pubkey endpoint per line), for shell tooling.

```bash
curl -s https://registry.libertynet.ai/peers
```

### `GET /api/v1/peers` <Status level="not_yet_wired" />

List peers (JSON envelope)

Returns 200 with a well-formed envelope, but the peer list inside is always empty — it is NOT the JSON form of /peers. Probed 2026-08-13: GET /peers returned 4+ real nodes with endpoints, while GET /api/v1/peers returned {"ok": true, "data": [], "error": null} at the same moment. Use /peers or /nodes until this is wired.

### `POST /api/v1/register` <Status level="implemented" />

Register a node

A node announces itself. Signature verification is enforced in grace mode.

### `POST /api/v1/heartbeat` <Status level="implemented" />

Refresh a node's last_seen

A node refreshes last_seen. Drives the `online` flag.

### `GET /nodes?callable=1` <Status level="implemented" />

Only nodes that are publicly reachable AND carry a registration signature. The honest answer to "who can I actually call".

```bash
curl -s https://registry.libertynet.ai/nodes?callable=1
```


## Node ↔ operator binding (double-signature)

**Base URL** `https://registry.libertynet.ai` · **Auth** ed25519 signatures; no API key

### `POST /v1/bindings/initiate` <Status level="implemented" />

Open a binding session (node side)

Node opens a binding session. Returns short_code + binding_token exactly once.

### `POST /v1/bindings/resolve` <Status level="implemented" />

Redeem a binding code (console side)

Console redeems the code/token and reads what the node is asking for.

### `POST /v1/bindings/authorize` <Status level="implemented" />

Authorize a binding (operator side)

Operator's device key signs an AuthorizationCredential.

### `GET /v1/bindings/{binding_session_id}/status` <Status level="implemented" />

Poll a binding session

Poll the session state machine.

### `POST /v1/bindings/{binding_session_id}/cancel` <Status level="implemented" />

Cancel a binding session

Abort a non-terminal session.

### `POST /v1/node/bindings/pending` <Status level="implemented" />

Poll for authorizations awaiting this node

Node polls for authorizations awaiting its acceptance. Signed poll.

### `POST /v1/node/bindings/{binding_session_id}/accept` <Status level="implemented" />

Accept a binding (node side)

Node accepts; registry re-verifies all four signatures before ACTIVE.

### `POST /v1/node/bindings/{binding_session_id}/reject` <Status level="implemented" />

Reject a binding (node side)

Node refuses the binding.


## Operator identity & session

**Base URL** `https://registry.libertynet.ai` · **Auth** challenge → device-key signature → bearer session (1h)

### `POST /v1/auth/challenge` <Status level="implemented" />

Get a login challenge

One-time challenge, 300s TTL.

### `POST /v1/auth/device-login` <Status level="implemented" />

Exchange a signed challenge for a bearer session

Exchange a signed challenge for a bearer session token.

### `GET /v1/operator/me/nodes` <Status level="implemented" />

List the nodes bound to you

Nodes bound to the authenticated operator, with live online/last_seen.

```bash
curl -s https://registry.libertynet.ai/v1/operator/me/nodes
```


## Credits & contribution evidence

**Base URL** `https://registry.libertynet.ai` · **Auth** bearer session

### `GET /v1/operator/me/credits` <Status level="not_yet_wired" />

Read your Credits balance

Endpoint is live and returns 200, but no credits ledger is connected. It returns settled/pending/estimated = 0 and "source": "not_yet_wired". Do not render these as earnings.

### `GET /v1/operator/me/evidence` <Status level="not_yet_wired" />

Read your contribution evidence

Live, returns count 0 and "source": "not_yet_wired". The read-only Evidence source is not connected.


## Oracle (on-chain)

**Base URL** `https://registry.libertynet.ai` · **Auth** on-chain (EIP-712 reporter signatures)

### `CONTRACT EvidenceOracle.submitEvidence` <Status level="testing" />

Solidity contract exists with a passing 23/23 suite (libertynet-oracle/, Phase 1). NOT deployed to any public network — there is no address to call.

### `CONTRACT PythPriceAdapter.getPrice` <Status level="testing" />

Pull-mode Pyth adapter with freshness guard. Same: tested, not deployed.

### `GET /v1/oracle/price` <Status level="planned" />

No HTTP oracle read surface exists yet.

### `POST /v1/oracle/report` <Status level="planned" />

Reporter submission over HTTP is not built. Reporters sign on-chain today.


## Agent wallet

**Base URL** `https://registry.libertynet.ai` · **Auth** n/a

### `POST /v1/wallet/create` <Status level="planned" />

Not built. Key material handling is unreviewed; do not stub it into user code.

### `POST /v1/wallet/session-key` <Status level="planned" />

Not built.

### `POST /v1/wallet/transfer` <Status level="planned" />

Not built. Value transfer is out of Genesis scope.


## Intent trading / DEX

**Base URL** `https://registry.libertynet.ai` · **Auth** n/a

### `POST /v1/dex/intent` <Status level="planned" />

Not built.

### `GET /v1/dex/quote` <Status level="planned" />

Not built.

### `POST /v1/dex/solve` <Status level="planned" />

Not built.

### `GET /v1/dex/pools` <Status level="planned" />

Not built.


## Canonical demo node

**Base URL** `https://libertynet.ai/demo-node` · **Auth** none

### `GET /health` <Status level="implemented" />

Registry liveness

Liveness, registration state, capabilities.

```bash
curl -s https://libertynet.ai/demo-node/health
```

### `GET /identity` <Status level="implemented" />

DID, public key (base58 and hex), fingerprint.

```bash
curl -s https://libertynet.ai/demo-node/identity
```

### `POST /echo` <Status level="implemented" />

Signs a caller-supplied nonce, so the reply is proof of possession rather than a replayable recording.

### `GET /verify-instructions` <Status level="implemented" />

The three verification steps, as data.

```bash
curl -s https://libertynet.ai/demo-node/verify-instructions
```

## Machine-readable

| File | What it is |
|---|---|
| [`/api-spec/status.json`](/api-spec/status.json) | The capability matrix. Source for every status on this page. |
| [`/api-spec/libertynet-v1.yaml`](/api-spec/libertynet-v1.yaml) | OpenAPI 3.1, with `x-ln-status` on each operation. |
| [`/ai-context.txt`](/ai-context.txt) | The same information as a paste-ready primer for an assistant. |
