# CLAUDE.md

> LibertyNet 项目 · 给 AI / Code 的常驻工程规则
> **版本：v1.0-FROZEN** · 2026-08-03
> 边界之内，最大自由；边界之上，绝不妥协。
> 这不是官僚流程，是让你跑得又快又不出大事的最小规矩。
> 适用：所有仓库（Node/Livo · Wallet/Field · DEX · Chain · LSP · LIO · LQX · Compute 等）
> 适用于所有 AI：Claude Code · Codex · Gemini · Cursor 等，遵循同一套工程规则。

---

## 1. 自主执行（最高原则）

不要停下来问琐碎问题。自己处理任何问题，一直做到完全完成为止。
只有碰到**红线**（钱、安全、冻结/治理内容）才停下来问 David。
其他情况，自己做合理判断，直接往下走。

**边界之内，你有最大的自主空间——放开做，不用请示。**

---

## 2. 红线（碰到必停，问 David）

只有这四类，停下来问：

1. **钱** — 动真实资金、改经济参数、碰 credits/结算/国库/payout
2. **安全** — 改密钥体系、动 Root Key、碰授权边界、降低安全等级
3. **治理** — 改宪法/协议/冻结文档、动核心权力边界
4. **不可逆的大动作** — 删数据、上主网、发布到生产、改标识/命名

**红线之外全是绿线**：写代码、测试、debug、部署测试环境、配服务器、写文档、常规开发——自己做，做完报告，不用中途请示。

---

## 3. 代码质量（和文档一样严）

1. **照规格实现** — 严格按架构和工程规格（边界/状态机/不变量），不自己发挥业务语义。想改设计 → 先说。
2. **守住不变量** — 规格里的"无 X 不得 Y"（无 Lease 不执行、无 Evidence 不结算、旧 Epoch 永久失效…），代码里必须真守住，不是写在注释里。
3. **必须有测试** — 每个功能有测试，达到规格的验收标准。没测试 = 没完成。
4. **生产级，不做残缺** — 不做 MVP 阉割版、不"beta 够用就行"。完整做。

---

## 4. 证据（自述完成不算）

完成的定义，不是你说"做完了"，是有证据：
- SHA commit / 部署日志 / 测试输出 / 交易哈希 / 真机验证
- **自述完成不算，CI 绿不算，PR 合并不算** —— 要真实、可验证、去重的证据
- 报告完成时附上证据，不附证据的"完成"不成立

---

## 5. 诚实（比好看重要）

- 没实现的，标"未实现 / not_yet_wired"，不假装完成
- 有问题的，标 BLOCKED，如实说，不掩盖
- 遇到"像 bug"的失败，先冷静排查确认根因，别侥幸合并
- 诚实的红 > 好看的假绿

---

## 6. 效率（别浪费 David 的注意力）

- 报告默认 3-5 行摘要 + "要详情吗？"，David 要才展开
- 不写没用的文档、不搞形式流程。第一性原理：每件事问"为什么要它"
- 一个任务做到完全完成，不半途反复请示

---

## 7. LQX 门禁（过质检才合并）

- 代码过 LQX（功能/安全/体验），不合格不合并
- 体验要达标（照 Living Language 设计系统），不只是功能能跑

---

## 8. 架构冻结（不碰的核心）

不改这些核心协议对象：SVRP · Intent · Capability · Lease · Evidence · Settlement · Identity。
要动 → 是红线，先问 David。

---

## 9. Reuse First（禁止重复造轮子）

修改前先搜索现有实现。
```
已有模块：优先复用 → 其次扩展 → 最后新建
```
未经充分理由，不得新增第二套实现。
（LibertyNet 模块已 70+，没这条后面必然重复开发、越来越乱。）

---

## 10. Compatibility（向后兼容）

不得无理由破坏：`API · Protocol · Storage · Wire Format`。
必须提供迁移方案。
（否则升级会把旧节点全部打断——违反"换十次实现文明不崩"的原则。）

---

## 11. Performance Budget（性能预算）

新增功能不得明显降低：`CPU · Memory · Network · Startup Time`。
超预算必须说明原因。
（节点软件 Livo 特别需要——机器资源有限。）

---

## 12. Dependency Policy（依赖原则）

优先：`标准库 → 已有依赖 → 成熟开源`。
禁止为小功能增加大型依赖。
（避免仓库越来越臃肿。）

---

## 13. Observability（可观测性）

新增模块必须提供：`Logs · Metrics · Health Check · Trace（适用时）`。
否则不算完成。
（接 LIO 观测平台就非常轻松。）

---

## 一句话

**边界之内最大自由（绿线自己做、放开发挥、做到完成）；边界之上绝不妥协（红线：钱/安全/治理/不可逆，必停问 David）。代码和文档一样严（照规格/守不变量/有测试/生产级）。完成要真实证据（自述不算）。诚实 > 好看。效率 > 官僚。复用优先、向后兼容、控性能预算、慎加依赖、自带可观测性。**

---

> LibertyNet · CLAUDE.md · **v1.0-FROZEN** · 2026-08-03
> 放进每个仓库根目录，所有 AI 每次自动读并遵守。
> 除非涉及四条红线，否则不频繁修改，保持全项目执行一致性。
