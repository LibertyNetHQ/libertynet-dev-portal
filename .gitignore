.DS_Store
*.egg-info/
__pycache__/
.pytest_cache/

dist/
out/
.next/
site/dist/
site/public/mcp/

# Playwright's browser and the root devDependency tree. The site, the SDKs and
# the MCP server all stay dependency-free; only the browser smoke test needs a
# browser, and nothing ships it.
node_modules/

.registry-watch/
p6-session.jsonl
